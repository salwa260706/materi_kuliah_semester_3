// array Destructuring
const nubmers = [1, 2, 3];
const [a, b, c] = nubmers;

console.log(a); // 1

// object Destructuring
const person = {
    name: 'John',
    age: 30     
}
const {name, age} = person;
console.log(name); // John